name: ci

on:
  push:
    branches:
      - "main"
  pull_request:
    branches:
      - "main"

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v2
      - name: Build the Docker Image
        run: docker build . --file Dockerfile --tag moodle-tg-bot:$(date +%s)